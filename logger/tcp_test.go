package logger

import (
	//	"encoding/json"
	"testing"
	//	"time"
)

func TestTcp(t *testing.T) {
	//	lg := NewLogger(10000)
	//	config := TcpLogConfig{"127.0.0.1", 10000}
	//	confbuf, _ := json.Marshal(config)
	//	lg.SetLogger(CONSOLE_PROTOCOL, "")
	//	lg.SetLogger(TCP_PROTOCOL, string(confbuf))
	//	lg.SetFuncDepth(2)
	//	lg.SetPrefix("tcptest")

	//	for i := 0; i <= 2; i++ {
	//		lg.Debug("DEBUG, %d", i)
	//		lg.Warn("Warn %d", i)
	//		lg.Error("Error %d", i)
	//	}
	//	time.Sleep(time.Second)
	//	lg.Close()
}
