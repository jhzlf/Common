package logger

import (
	//"encoding/json"
	"testing"
	//"time"
)

func TestUdpLog(t *testing.T) {
	//	lg := NewLogger(10000)
	//	lg.SetLogger(CONSOLE_PROTOCOL, "")

	//	config := UdpLogConfig{"127.0.0.1", 17777}
	//	confbuf, _ := json.Marshal(config)
	//	lg.SetLogger(UDP_PROTOCOL, string(confbuf))
	//	lg.SetFuncDepth(2)
	//	lg.SetPrefix("1")

	//	for i := 0; i <= 2; i++ {
	//		lg.Debug("DEBUG, %d", i)
	//		lg.Warn("Warn %d", i)
	//		lg.Error("Error %d", i)
	//	}
	//	time.Sleep(time.Second)
	//	lg.Close()
}
